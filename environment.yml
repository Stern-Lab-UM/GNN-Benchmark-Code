name: gnn-benchmark
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - pyyaml
  - click
  - tqdm
  - tensorboard
  - nbformat
  - nbconvert
  - pip:
      - hdf5storage
      # Install PyTorch separately with scripts/setup_lh_env.sh or with the
      # official PyTorch command for the target CPU/CUDA platform.
